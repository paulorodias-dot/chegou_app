/*
 * Sistema Chegou!
 * Central de Encomendas — Entrada Oficial
 *
 * Camada reservada para acesso aos contratos oficiais da Entrada.
 *
 * IMPORTANTE:
 * Nesta fase estrutural nenhuma RPC, tabela, Edge Function
 * ou consulta Supabase deve ser criada ou chamada por este arquivo.
 *
 * Quando a fronteira Recebimento → Entrada for homologada,
 * esta camada passará a consumir somente os contratos autorizados.
 */

export const entradaService = Object.freeze({});

export default entradaService;