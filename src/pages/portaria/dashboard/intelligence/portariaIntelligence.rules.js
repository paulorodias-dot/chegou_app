export function construirInteligenciaPortaria({
  dados,
} = {}) {
  if (!dados) {
    return [];
  }

  /*
   * Futuramente:
   * regras estatísticas reais;
   * Motor de Insights Operacionais;
   * CSIC;
   * IA contextual.
   *
   * Nenhuma recomendação deve ser inventada.
   */

  return [];
}